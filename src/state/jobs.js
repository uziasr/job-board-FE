const initialState = {

}

const jobsReducer = (state=initialState, action) => {
    switch (action) {
        case "":{
            return {}
        }
        default: {
            return initialState
        } 
    } 
}